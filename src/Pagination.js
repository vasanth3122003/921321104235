// Pagination.js
import React from 'react';

const Pagination = () => {
  return (
    <div className="pagination">
      {/* Pagination buttons */}
    </div>
  );
};

export default Pagination;
